"use client"; import React, { useState, useRef } from 'react'; import { Upload, X, FileText, Loader2, CheckCircle2 } from 'lucide-react'; import { authService } from '@/src/services/auth.service'; interface FileUploadProps { onUploadSuccess: (url: string) => void; accept?: string; label?: string; maxSizeMB?: number; } export default function FileUpload({ onUploadSuccess, accept = ".pdf,.docx,.doc", label = "Tải lên tài liệu", maxSizeMB = 10 }: FileUploadProps) { const [file, setFile] = useState<File | null>(null); const [uploading, setUploading] = useState(false); const [uploadedUrl, setUploadedUrl] = useState<string | null>(null); const [error, setError] = useState<string | null>(null); const fileInputRef = useRef<HTMLInputElement>(null); const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => { const selected = e.target.files?.[0]; if (!selected) return; if (selected.size > maxSizeMB * 1024 * 1024) { setError(`Dung lượng file quá lớn (Max ${maxSizeMB}MB)`); return; } setFile(selected); setError(null); startUpload(selected); }; const startUpload = async (selectedFile: File) => { setUploading(true); const formData = new FormData(); formData.append('file', selectedFile); try { const token = authService.getToken(); const res = await fetch('/api/storage/upload', { method: 'POST', headers: { 'Authorization': `Bearer ${token}` }, body: formData }); if (res.ok) { const data = await res.json(); setUploadedUrl(data.url); onUploadSuccess(data.url); } else { setError("Tải lên thất bại, vui lòng thử lại."); } } catch (err) { setError("Lỗi kết nối máy chủ lưu trữ."); } finally { setUploading(false); } }; return ( <div className="space-y-2"> <div onClick={() => !uploading && fileInputRef.current?.click()} className={`p-6 rounded-[32px] border-2 border-dashed transition-all flex flex-col items-center justify-center text-center space-y-3 cursor-pointer group ${ uploadedUrl ? 'bg-emerald-500/5 border-emerald-500/30' : 'bg-card border-white/10 hover:bg-card hover:border-primary/50' }`} > <input type="file" ref={fileInputRef} className="hidden" accept={accept} onChange={handleFileChange} /> {uploading ? ( <Loader2 className="w-10 h-10 text-primary animate-spin" /> ) : uploadedUrl ? ( <CheckCircle2 className="w-10 h-10 text-emerald-500" /> ) : ( <div className="w-12 h-12 rounded-2xl bg-primary/10 text-primary flex items-center justify-center group-hover:scale-110 transition-transform"> <Upload className="w-6 h-6" /> </div> )} <div> <p className="text-xs font-bold text-foreground"> {uploadedUrl ? "Đã tải lên thành công" : file ? file.name : label} </p> <p className="text-[10px] text-muted-foreground mt-1"> {uploadedUrl ? "File đã sẵn sàng trong hồ sơ" : `Định dạng: ${accept.split(',').join(' ')} (Max ${maxSizeMB}MB)`} </p> </div> </div> {error && <p className="text-[10px] text-red-500 font-bold ml-2">{error}</p>} </div> ); } 